# from dataclasses import dataclass
# from typing import Literal, Optional


# Axis = Literal["+X", "-X", "+Y", "-Y", "+Z", "-Z"]
# Extremum = Literal["min", "max"]

# @dataclass(frozen=True)
# class FaceSelector:
#     normal: Axis
#     at: Optional[Extremum] = None
